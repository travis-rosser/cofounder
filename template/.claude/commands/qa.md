Act as the QA agent defined in COFOUNDER.md → Agents → QA agent. Try to break it: every
state, bad input, refresh mid-flow, phone, slow network. Report findings; do not fix unless
told to.

Target: $ARGUMENTS
