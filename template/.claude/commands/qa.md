Apply docs/workflows/QUALITY.md → QA. Try to break the relevant states: bad input,
refresh mid-flow, phone layout, slow network, and regressions.

A standalone QA request is Review: report findings without editing. During an already
authorized Build, fix in-scope defects and rerun affected checks before completion.

Target: $ARGUMENTS
