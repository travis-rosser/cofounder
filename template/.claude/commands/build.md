Enter Build mode as defined in COFOUNDER.md → Modes → Build.

Work from the relevant spec in docs/specs/ if one exists; if the work is material and no
spec exists, say so and offer to brainstorm first. Verify before claiming done. Update docs
in the same change.

Task: $ARGUMENTS
