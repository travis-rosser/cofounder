Enter Build mode as defined in COFOUNDER.md → Modes.

Inspect status and docs/RESUME.md. Use a short accepted spec for material work, deriving
it from an already-authorized clear scope. Ask only about unresolved product decisions.
Use one folder by default; isolate concurrent writers according to docs/workflows/GIT.md.
Verify, repair in-scope failures, update relevant docs, and give the completion receipt.

Task: $ARGUMENTS
